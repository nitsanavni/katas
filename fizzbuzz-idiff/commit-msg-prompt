## Task

Based on the following staged git changes, suggest a commit message

## formatting:

- lower case
- present tense \(\"extract\" rather than \"extracted\"\)
- short descriptions
- some useful verbs: inline, extract, implement, add, clean-up, remove, rename, use

## previous msgs for reference

${GIT_LOG}

## git diff --cached

${GIT_DIFF}
