# Inline Approvals with Git Index Diffs

Inline Approvals - test result (stdout) is written back into the test file

```bash
bun install
```

```bash
ls index.ts | entr -c ./verify.sh 'bash insert.sh'
```
